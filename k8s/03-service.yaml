apiVersion: v1
kind: Service
metadata:
  name: nl2sql-service
  namespace: default
  labels:
    app: nl2sql-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: nl2sql-app